# Research Paper Answer Bot - Requirements
# Created by Daniel Ojeda Rosales

# Core Streamlit
streamlit>=1.28.0

# Data manipulation and analysis
pandas>=1.5.0
numpy>=1.24.0

# Visualization
plotly>=5.15.0
# Explicitly adding some plotly dependencies that might cause issues
# or ensuring compatible versions
tenacity>=8.0.0 # Plotly dependency
jinja2>=3.0.0 # Plotly dependency
jsonschema>=4.0.0 # Plotly dependency
matplotlib>=3.7.0 # Included for potential local visualization/testing
seaborn>=0.12.0 # Included for potential local visualization/testing

# Machine Learning and NLP
scikit-learn>=1.3.0
sentence-transformers>=2.2.0 # Used for some embedding models

# Vector Database
chromadb>=0.4.0

# OpenAI API
openai>=1.0.0

# Web scraping and search (Task 7)
serpapi>=0.1.0 # Ensure SerpAPI is included
requests>=2.31.0
beautifulsoup4>=4.12.0
newspaper3k>=0.2.8 # Used for article parsing

# PDF processing
PyPDF2>=3.0.0 # Used for document loading

# Text processing
rank-bm25>=0.2.2 # Used for keyword retrieval
textstat>=0.7.0 # Used for text analysis/readability

# Date handling
python-dateutil>=2.8.0 # Used for date parsing

# Additional utilities (often included by other packages but good to list explicitly)
typing-extensions>=4.0.0
pydantic>=1.10.0 # Often a dependency of other libraries

# If you used any other specific libraries in your Task 1-7 code,
# make sure to add them here with their version specifiers.
# Example: faiss-cpu if you used FAISS for vector storage
# Example: PyTorch or TensorFlow if directly used for models
